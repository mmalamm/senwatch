{
  "name": "senwatch_data",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-gui": "npm install --prefix ./gui/ && npm run build --prefix ./gui/",
    "start": "npm run build --prefix ./gui/ && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "serve-favicon": "^2.5.0"
  }
}
